namespace GameOfLife.Api.Service;

/// <summary>
/// Service layer: owns the Game of Life rules and orchestration (create a board,
/// step to the next state, jump N states, compute the final state). Methods to be
/// designed. The Web layer depends only on this interface, never on a concrete type.
/// </summary>
public interface IBoardService
{
}
